const SinglyLinkedList = require("../main");

describe("test my SinglyLinkedList", () => {

    it("instantiates an object", () => {
        const list = new SinglyLinkedList();

        expect(list).not.toBeNull();
    });

    it("append test",() => {
        const list = new SinglyLinkedList();

        expect(list).not.toBeNull();
    })
});